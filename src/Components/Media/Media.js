import { Box, Typography } from '@mui/material';
import React from 'react';

const Media = () => {
    return (
        <Box>
            <Typography color={'white'}>hdk</Typography>
            
        </Box>
    );
}

export default Media;
